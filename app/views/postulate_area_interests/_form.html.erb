<%= form_for(postulate_area_interest) do |f| %>
  <% if postulate_area_interest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(postulate_area_interest.errors.count, "error") %> prohibited this postulate_area_interest from being saved:</h2>

      <ul>
      <% postulate_area_interest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :area_interest_id %>
    <%= f.text_field :area_interest_id %>
  </div>

  <div class="field">
    <%= f.label :postulate_id %>
    <%= f.text_field :postulate_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
