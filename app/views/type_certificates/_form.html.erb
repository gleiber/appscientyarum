<%= form_for(type_certificate) do |f| %>
  <% if type_certificate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type_certificate.errors.count, "error") %> prohibited this type_certificate from being saved:</h2>

      <ul>
      <% type_certificate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %>
    <%= f.text_field :descripcion %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
