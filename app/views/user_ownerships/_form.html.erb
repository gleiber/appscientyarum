<%= simple_form_for(@user_ownership) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :user %>
    <%= f.association :ownership %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
