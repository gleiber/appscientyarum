<%= simple_form_for(@survey_response) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :poll %>
    <%= f.input :sexo %>
    <%= f.input :edad %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
