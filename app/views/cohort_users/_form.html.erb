<%= form_for(cohort_user) do |f| %>
  <% if cohort_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cohort_user.errors.count, "error") %> prohibited this cohort_user from being saved:</h2>

      <ul>
      <% cohort_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cohort_id %>
    <%= f.text_field :cohort_id %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
