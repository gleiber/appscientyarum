<%= form_for(@request_response) do |f| %>
  <% if @request_response.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@request_response.errors.count, "error") %> prohibited this request_response from being saved:</h2>

      <ul>
      <% @request_response.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type_req_resp_id %><br>
    <%= f.text_field :type_req_resp_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
