<%= form_for(why_dcyt) do |f| %>
  <% if why_dcyt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(why_dcyt.errors.count, "error") %> prohibited this why_dcyt from being saved:</h2>

      <ul>
      <% why_dcyt.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %>
    <%= f.text_field :descripcion %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
