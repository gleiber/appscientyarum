<%= form_for(@type_poll_role) do |f| %>
  <% if @type_poll_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_poll_role.errors.count, "error") %> prohibited this type_poll_role from being saved:</h2>

      <ul>
      <% @type_poll_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type_poll_id %><br>
    <%= f.text_field :type_poll_id %>
  </div>
  <div class="field">
    <%= f.label :role_id %><br>
    <%= f.text_field :role_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
