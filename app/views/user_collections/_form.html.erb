<%= form_for(@user_collection) do |f| %>
  <% if @user_collection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_collection.errors.count, "error") %> prohibited this user_collection from being saved:</h2>

      <ul>
      <% @user_collection.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :collection_id %><br>
    <%= f.text_field :collection_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
