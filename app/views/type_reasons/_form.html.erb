<%= form_for(@type_reason) do |f| %>
  <% if @type_reason.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_reason.errors.count, "error") %> prohibited this type_reason from being saved:</h2>

      <ul>
      <% @type_reason.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_field :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
