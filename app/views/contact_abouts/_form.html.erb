<%= form_for(contact_about) do |f| %>
  <% if contact_about.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contact_about.errors.count, "error") %> prohibited this contact_about from being saved:</h2>

      <ul>
      <% contact_about.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %>
    <%= f.text_field :descripcion %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
