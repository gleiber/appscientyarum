<%= form_for(@user, url: "create_user_titulos", :html => {:class => 'form'}) do |f| %>
      <div class="row header" style="background-color: #1f4bae; margin-top:50px;">
        <h1 class="center-align col-lg-12 text-center"><b>Seleccione Sus Titulos Academicos</b></h1>
      </div>
<div class="container">
        <div class="row" style="margin-top:50px;">

          <%   @ownerships.each do |ownership| %>
            <div class="col-md-4 text-left" style="margin-top:20px;">
              <%= check_box_tag "ownerships[]", ownership.id, @user.ownerships.include?(ownership ) %> <%= ownership.academic_degree.descripcion+ ' '+ ownership.speciality.descripcion %>
            </div>
          <% end %>
        </div>
      </div>>
  <div class="container">
    <div class="row" >
        <div class="form-group col-md-2 col-md-offset-3" style="margin-top: 30px;">
          <%= link_to edit_user_path(current_user), class: "btn btn-warning", style:"width: 100px;" do %>
            <span class="fa fa-reply"></span> Atras    
          <% end %>
        </div>
        <div class="form-group col-md-2 col-md-offset-1" style="margin-top: 30px;">
          <%= button_to "", class: "btn btn-primary ", style:"width: 100px;" do %>
            <span class="fa fa-floppy-o"></span> Agregar    
          <% end %>
        </div>
      </div>
  </div>
<% end %>
<style type="text/css">
  .container {
    width: auto !important;
  }
</style>