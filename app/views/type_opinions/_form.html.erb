<%= form_for(@type_opinion) do |f| %>
  <% if @type_opinion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_opinion.errors.count, "error") %> prohibited this type_opinion from being saved:</h2>

      <ul>
      <% @type_opinion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_field :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
