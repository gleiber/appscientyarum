
    <div class="row z-depth-2" style="margin-top:20px;">


    <div class="col-md-10 col-md-offset-1 table-responsive col-sm-12 col-xs-12" >
     <div class="row header" style="background-color: #1f4bae;">
     <h1 class="center-align col-lg-12 text-center"><b> Periodos </b></h1>
     </div>
     <div class="text-center">
    <%= link_to new_period_path, {class:"btn btn-success", style: "margin: 25px auto;" } do %>
    <i class="fa fa-plus"></i> Registrar Periodo
    <% end %>
    </div>
        <table id="tabla_vp" class="table table-hover text-center">
          <thead>
            <tr>
              <th class="text-center">Fecha de Inicio Periodo</th>
              <th class="text-center">Fecha de Final Periodo</th>
              <th  class="text-center" colspan="3">Acción</th>
            </tr>
          </thead>
          <tbody class="searchable">
            <% @periods.each do |period| %>
              <tr>
                <td><%= period.fecha_desde %></td>
                <td><%= period.fecha_hasta %></td>
                <td><%= link_to edit_period_path(period),class: "btn btn-info" do %>
                      <i class="fa fa-pencil-square-o" aria-hidden="true"></i>   
                    <% end %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  
<style type="text/css">
  form{
    background-color:transparent !important; 
    box-shadow: none !important;
  }
</style>















