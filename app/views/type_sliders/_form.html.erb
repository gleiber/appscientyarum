<%= form_for(@type_slider) do |f| %>
  <% if @type_slider.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_slider.errors.count, "error") %> prohibited this type_slider from being saved:</h2>

      <ul>
      <% @type_slider.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_field :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
