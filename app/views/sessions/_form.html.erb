<%= form_for(@session) do |f| %>
  <% if @session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@session.errors.count, "error") %> prohibited this session from being saved:</h2>

      <ul>
      <% @session.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :classroom_laboratory_schedule_id %><br>
    <%= f.text_field :classroom_laboratory_schedule_id %>
  </div>
  <div class="field">
    <%= f.label :coh_per_sub_u_id %><br>
    <%= f.text_field :coh_per_sub_u_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
