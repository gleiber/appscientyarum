<%= form_for(@collection_program) do |f| %>
  <% if @collection_program.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@collection_program.errors.count, "error") %> prohibited this collection_program from being saved:</h2>

      <ul>
      <% @collection_program.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :program_id %><br>
    <%= f.text_field :program_id %>
  </div>
  <div class="field">
    <%= f.label :collection_id %><br>
    <%= f.text_field :collection_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
