<%= form_for(@type_poll) do |f| %>
  <% if @type_poll.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_poll.errors.count, "error") %> prohibited this type_poll from being saved:</h2>

      <ul>
      <% @type_poll.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_field :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
