<%= form_for(program_subject) do |f| %>
  <% if program_subject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(program_subject.errors.count, "error") %> prohibited this program_subject from being saved:</h2>

      <ul>
      <% program_subject.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :subject_id %>
    <%= f.text_field :subject_id %>
  </div>

  <div class="field">
    <%= f.label :program_id %>
    <%= f.text_field :program_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
