<%= form_for(regimen_duration, remote: true) do |f| %>
  <% if regimen_duration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(regimen_duration.errors.count, "error") %> prohibited this regimen_duration from being saved:</h2>

      <ul>
      <% regimen_duration.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :program_id %>
    <%= f.text_field :program_id %>
  </div>

  <div class="field">
    <%= f.label :numero %>
    <%= f.text_field :numero %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
